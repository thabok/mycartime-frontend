# Optional: override the backend origin the frontend talks to. If unset,
# it defaults to http://<current-page-hostname>:1338 (see backend/src/config.py PORT).
# VITE_BACKEND_URL=http://localhost:1338
